/* Продавец и контакты - одна копия на весь сайт: документы (`src/LegalPage.tsx`),
   подвал каждой страницы (`src/support/SiteFooter.tsx`) и окно поддержки.

   Требование модерации Робокассы (robokassa.com/how-works): «данные
   самозанятого размещены в подвале сайта» в виде «Самозанятый ФИО
   (полностью), ИНН», а почта и телефон для связи не скрыты. Закон о защите
   прав потребителей требует того же: покупатель узнаёт продавца до оплаты.

   Почта - личный ящик владельца. Здесь стояли legal@ и
   support@homeworkcopilot.ru, а у домена нет ни одной MX-записи: письма не
   доходили никуда. Адрес на домене появится одной строкой, когда заведут MX
   (аудит 16 сентября 2026, Г8); выдумывать его раньше нельзя. Тот же адрес -
   в `public/.well-known/security.txt`.

   ИНН вписан 14 сентября 2026 со слов владельца, контрольные цифры сходятся.
   Пустая строка прячет ИНН везде: выдуманный или «будет указан» на
   юридической странице был бы враньём. */
export const sellerFullName: string = 'Кандеев Роман Александрович'
export const sellerInn: string = '745312205102'
export const contactEmail = 'roman.kandeevy@gmail.com'
export const contactPhone = '+7 995 125-79-75'

/* Номер для `tel:` - только цифры и плюс. */
export const contactPhoneHref = `tel:${contactPhone.replace(/[^\d+]/g, '')}`

/* ИНН внутри фразы: «, ИНН 123456789012» или ничего. Номер держится за
   «ИНН» неразрывным пробелом: на телефоне подвал переносил число отдельно. */
export const sellerInnClause = sellerInn ? `, ИНН\u00a0${sellerInn}` : ''
